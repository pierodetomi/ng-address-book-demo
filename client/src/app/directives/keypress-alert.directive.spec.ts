import { KeypressAlertDirective } from './keypress-alert.directive';

describe('KeypressAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new KeypressAlertDirective();
    expect(directive).toBeTruthy();
  });
});
